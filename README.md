# УПАЙ — 3D-прототип v0.1

Технический прототип традиционной кыргызской игры «Упай», адаптированной под
механику моментальной электронной лотереи X2 LOTO. Реализовано на
[Babylon.js](https://www.babylonjs.com/) + [Havok Physics](https://www.havok.com/).

Статус: базовая механика зафиксирована (см.
`docs/UPAY_v0.1_Technical_Specification.docx`).
Приоритет v0.1 — физика чүкө, корректное рассыпание, определение четырёх
положений (AYKUR/TAA/CHIK/BOK) и управляемый точный щелчок одного чүкө в
другой. Финальный визуальный стиль, модели, музыка и интеграция с LMS —
вне scope этой версии (см. §17 ТЗ).

## Запуск

Требуется Node.js 18+.

```bash
npm install
npm run dev      # dev-сервер (Vite) с горячей перезагрузкой
```

Откройте адрес, который выведет Vite (обычно `http://localhost:5173`).
Интерфейс mobile-first: лучше всего смотрится в вертикальной ориентации
на узком экране (эмулятор телефона в DevTools или реальное мобильное
устройство в локальной сети через `--host`).

```bash
npm run build     # production-сборка в dist/
npm run preview   # локальный просмотр production-сборки
```

Havok физика загружается как WASM через `@babylonjs/havok`; интернет не
нужен после `npm install` — всё собирается локально через Vite.

## Управление

- Кнопка **«РАССЫПАТЬ»** запускает новое рассыпание 15 чүкө + Хана.
- **Tap-to-target**: тап по чүкө делает его источником, допустимые цели
  (то же положение) подсвечиваются зелёным — тап по цели выполняет щелчок.
- **Swipe**: проведите пальцем/мышью от чүкө в сторону цели — направление
  свайпа сопоставляется с проекциями допустимых целей на экран.
- Выпадающий список **«Сценарий»** переключает тестовый сценарий
  (`ZERO` / `ONE` / `KHAN`) перед следующим рассыпанием.
- Чекбоксы внизу — debug-режимы: FPS, подписи положений над чүкө,
  wireframe вместо физических тел (упрощённая визуализация коллайдеров).

## Структура проекта

```
index.html               Контейнер приложения, canvas, минимальная разметка + UI
src/main.js               Bootstrap: Engine, сцена, физика, игровой цикл, UI-обвязка
src/scene.js               Камера, свет, поле, невидимые физические границы
src/physics.js              Havok: инициализация, rigid body, sleep/stability
src/chuko.js                 Процедурная модель обычного чүкө + hitProxy + физика
src/khan.js                   Хан: isKhan=true, khanUnlocked
src/scatter.js                 Рассыпание: позиции/высоты/вращения/импульсы
src/orientation.js              Классификация AYKUR/TAA/CHIK/BOK
src/layout-validator.js          Проверка раскладки (границы/стопки/видимость/
                                  доступность Хана/пары/дистанция) + условия reroll
src/pair-selector.js              Выбор source/target, подсветка
src/input.js                       Tap/swipe → pointer events → flick
src/flick.js                        Импульс щелчка + коррекция траектории в полёте
src/scenario-engine.js               ZERO/ONE/KHAN, сбор троек, разблокировка Хана
src/game-state.js                     Машина состояний партии
src/config.js                          Все настраиваемые параметры (см. ниже)
assets/models/, assets/textures/        Заготовки под будущие ассеты (пока пусто)
```

Порядок разработки следовал §3 ТЗ: сцена → базовый чүкө → рассыпание →
стабилизация физики → определение положения → валидатор раскладки →
выбор пары → щелчок → сценарии.

## Модель чүкө (временная геометрия)

Финальных 3D-моделей в v0.1 нет (см. §17 ТЗ). Чтобы физически повторить
поведение настоящей астрагалы (4 устойчивых положения, а не 6 как у куба),
чүкө строится процедурно (`src/chuko.js`) как прямоугольный блок,
вытянутый по локальной оси X, со скруглёнными полусферическими торцами
(через `CSG.union`). У такой формы ровно 4 плоские устойчивые грани
(±Y, ±Z) — скруглённые концы не дают устойчиво «встать на попа».
`src/orientation.js` классифицирует положение, сравнивая мировую ось Y с
этими четырьмя локальными нормалями.

Видимая геометрия мала на экране (особенно на телефоне), поэтому у
каждого чүкө есть невидимый (но пикающийся, `alpha=0`) `hitProxy` —
дочерняя сфера увеличенного диаметра для надёжного tap/swipe-попадания;
сам видимый меш `isPickable = false`.

## config.js — параметры

Все числовые параметры вынесены в `src/config.js` (см. §15 ТЗ), логику
менять не нужно, чтобы подстроить баланс/физику/UI:

- `chukoCount`, `chukoScale`, `khanScale`, `chukoSize` (включая `hitProxyDiameter`)
- `physics` — масса, friction, restitution, linear/angular damping, гравитация
- `workArea` — размер игровой зоны и высота невидимых границ
- `ui.bottomReservedFraction` — доля экрана снизу под UI (камера туда не рендерится)
- `scatter` — радиус/высота/джиттер/импульс рассыпания
- `stability` — пороги скорости и число кадров для признания «уснувшим»
- `layoutValidator` — допуски валидатора раскладки и лимит попыток reroll
- `flick` — сила щелчка, коррекция траектории, вариативность вращения
- `highlight` — цвета/интенсивность подсветки source/target
- `input` — пороги свайпа (дистанция/время/направление)
- `scenario` — сценарий по умолчанию, число пар на один Упай
- `debug` — FPS, физ. тела, подписи положений, траектория

## Правила тестирования

Формальных автотестов в v0.1 нет (см. §17 ТЗ — не входит в объём).
Ручная проверка перед коммитом/демо, в порядке критериев готовности (§16 ТЗ):

1. `npm run dev`, открыть в мобильном viewport (вертикальная ориентация).
2. Нажать «РАССЫПАТЬ» несколько раз подряд — раскладка должна каждый раз
   стабилизироваться (`stableFrames ≥ minStableFrames`) и провалидироваться
   без бесконечных reroll (в консоли предупреждение появляется только если
   `maxRerollAttempts` исчерпан — это сигнал, что параметры валидатора или
   камеры разошлись с реальной физикой, и параметры нужно поправить в `config.js`).
3. Включить debug-подписи положений — убедиться, что классификация
   AYKUR/TAA/CHIK/BOK выглядит стабильно (не «дёргается» между кадрами
   на уснувших объектах).
4. Выбрать source (тап) — допустимые target должны подсветиться зелёным;
   тап по недопустимой цели/пустому месту снимает выбор без штрафа.
5. Проверить оба режима щелчка: tap-to-target и swipe.
6. Прогнать все три сценария (`ZERO` / `ONE` / `KHAN`) через селектор и
   убедиться, что счётчик пар, формирование Упая и разблокировка Хана
   отражаются в статусной строке.
7. Проверить FPS (debug-чекбокс) на целевом устройстве/эмуляторе —
   просадки ниже ~60 FPS диагностировать до работы над визуальными
   эффектами (§16 ТЗ).

При правке физических параметров (`physics`, `scatter`, `stability`)
после каждого изменения повторяйте шаги 2–3: нестабильные damping/friction
проявляются как «дрожащие» или не засыпающие объекты.
